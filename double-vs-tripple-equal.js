// ! == checks only the values are similar or not.

const one = 1;
const two = true;

if (one == two) {
    console.log("True");
} else {
    console.log("False");
}

// ! === checks the values are similar or not with their Data Type.

const first = 2;
const second = '2';

if (one === two) {
    console.log("True");
} else {
    console.log("False");
}
